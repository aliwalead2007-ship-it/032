--- AGENTS.md
+++ AGENTS.md
@@ -73,5 +73,5 @@
 > المسار محصَّن في الكود؛ آية الدخول والسبلاش جاهزان.
 > **الأولوية:** نتيجة اختبار التصدير على الجهاز → ثم إصلاح أو تقوية offline فقط.
 > تم إصلاح `analyzeIdea` بحيث يستخرج النبرة والمحاور بصيغة JSON صارمة بدون قوالب، وتم تفادي استخدام محتوى وهمي في `ProcessingScreen`، وتنقيح نماذج البيانات.
 > وثّق كل حركة في `AGENTS.md` + `README.md`.
